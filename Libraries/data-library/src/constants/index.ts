export const DEFAULT_DECIMAL_PRECISION: number = 10;

export const MIN_DECIMAL_PRECISION: number = 1;

export const MAX_DECIMAL_PRECISION: number = 20;

export const DEFAULT_DECIMAL_SCALE: number = 2;

export const MIN_DECIMAL_SCALE: number = 0;

export const MAX_DECIMAL_SCALE: number = 10;

export const DEFAULT_STRING_LENGTH: number = 64;

export const MIN_STRING_LENGTH: number = 1;

export const MAX_STRING_LENGTH: number = 1024;

export const DEFAULT_DATE_FORMAT: string = 'DD-MM-YYYY';

export const DEFAULT_TIME_FORMAT: string = 'HH:mm:ss';

export const DEFAULT_DATE_TIME_FORMAT: string = `${DEFAULT_DATE_FORMAT} ${DEFAULT_TIME_FORMAT}`;

export const DEFAULT_TIMESTAMP_FORMAT: string = `${DEFAULT_DATE_FORMAT}T${DEFAULT_TIME_FORMAT}`;
