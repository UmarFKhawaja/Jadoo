{
  "name": "@jadoo/project",
  "version": "0.0.0",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "test": "lerna run test",
    "clean": "lerna run clean"
  },
  "workspaces": [
    "Libraries/**",
    "Modules/**",
    "Plugins/**"
  ],
  "devDependencies": {
    "glob": "^10.3.10",
    "lerna": "^8.0.0"
  }
}
