{
  "name": "@jadoo/project",
  "version": "0.0.0",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "test": "lerna run test",
    "clean": "lerna run clean"
  },
  "workspaces": [
    "Libraries/*",
    "Modules/*",
    "Plugins/*"
  ],
  "devDependencies": {
    "lerna": "^8.0.0"
  }
}
