{
  "name": "@jadoo/project",
  "version": "0.0.0",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "test": "lerna run test",
    "clean": "lerna run clean"
  },
  "devDependencies": {
    "lerna": "^6.6.2"
  }
}
